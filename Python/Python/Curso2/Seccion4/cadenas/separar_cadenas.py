# Separar cadenas

datos = 'hola mundo'
lista = datos.split() # por default separa cada elemento por espacion en blanco
print(lista)

datos = 'Ruben,24,Guatemala'
lista = datos.split(',')
print(lista)